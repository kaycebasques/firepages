{
  "name": "server",
  "version": "0.0.0",
  "private": true,
  "devDependencies": {
    "firebase-tools": "^12.7.0"
  },
  "scripts": {
    "dev": "npx firebase emulators:start",
    "deploy": "npx firebase deploy --only functions"
  }
}
